# Railway deployment configuration
# This file tells Railway how to run your application

# Railway will automatically detect Python and install dependencies
# Make sure requirements_production.txt exists

# Environment variables needed:
# OPENAI_API_KEY=your_openai_api_key

# Railway will automatically:
# 1. Install Python dependencies from requirements_production.txt
# 2. Run the Procfile command
# 3. Expose your app on a public URL

